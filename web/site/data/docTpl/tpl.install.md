Как таковой инсталяции фреймворка в ngn нет, а если вас всё же интересует этот момент, то.
Установите php5.5 и скачайте себе его.

Всё должно быть в /home/user/ngn-env, где user - это пользователь из под которого запускается веб-сервер (если такой есть)

Далее. для работы нам сразу понадобиться пакет `run`

Слой php-кода без настроек. голый CLI:

run "(new Class)->method()" yoLib

Слой проекта:

Создаем файл config/database.php: ...

Инсталируем пакет pm
pm localServer createProject test test.server.ru common

готово